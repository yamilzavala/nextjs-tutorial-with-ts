import { Form, UsersList } from "@/components";

const ActionsPage = () => {
    return (
        <div>
            <Form/>
            <UsersList/>
        </div>
    );
};

export default ActionsPage;